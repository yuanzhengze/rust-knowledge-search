# 开发日志

本文档用于记录项目开发过程，方便后续编写实验报告、整理成员贡献和回顾实现过程。

## 2026-05-31：项目启动

已完成：

- 确定选题：基于 Rust 的本地知识库语义搜索系统。
- 确定小组成员：
  - 袁正泽
  - 谭张锐
  - 邱俊杰
  - 黄开轩
  - 陈文涛
- 制定两周开发计划。
- 创建项目协作文档。
- 创建仓库级 Agent 协作规范。
- 补充 README、贡献规范、测试计划、接口设计和演示脚本等协作文件。

待完成：

- 初始化 Rust Cargo 项目。
- 创建 `src/` 模块结构。
- 确定核心数据结构最终版本。
- 开始文件扫描模块开发。

## 2026-06-02：完成文件扫描模块

已完成：

- 实现 `scanner::scan_documents`，使用 `walkdir` 递归遍历目录，过滤 `.md` / `.txt`（扩展名大小写不敏感），跳过隐藏文件、隐藏目录与 `target/`。
- 路径不存在或非目录时返回 `AppError::InvalidPath`；`walkdir::Error` 中的 IO 错误透传为 `AppError::Io`，目录权限问题不再被静默吞掉。
- 在 `scanner.rs` 同文件下补 8 个单测，覆盖 `docs/测试计划.md` §3.1 全部用例：示例目录正向扫描、不支持扩展名过滤、缺失路径报错、传入文件路径报错、空目录返空、隐藏条目剪枝、`target/` 剪枝、元数据字段完整性。
- 单测内置零依赖临时目录工具（基于进程 PID + 纳秒时间戳 + 原子计数器），不引入 `tempfile` 依赖。
- 本地校验：`cargo build` / `cargo test`（scanner 8/8 通过，集成测试保持 `#[ignore]`）/ `cargo clippy --all-targets -- -D warnings` / `cargo fmt --check` 全部通过。

遇到的问题：

- `walkdir::Error::into_io_error` 返回 `Option<io::Error>`，需要兜底以避免在拿不到 IO 错误时丢失上下文，最终在元数据读取失败路径上回退到 `AppError::InvalidPath("metadata read failed")`。
- `filter_entry` 在 `depth == 0` 时也会触发，若 `root` 自身以 `.` 开头会被整体剪掉，因此 `is_hidden` 加了 `depth() > 0` 守卫。

下一步：

- 陈文涛：基于本模块产出的 `DocumentMeta`，开始 `parser::parse_document`（Day 3，6/2）。
- 后续 chunker / indexer / search 沿关键路径推进。

相关提交：

- 待 `feature/scanner` 分支提交。

## 2026-06-02：完成索引与搜索模块（提前覆盖 Day 5 / Day 6）

已完成：

- 实现 `indexer::InvertedIndex::build` 与 `lookup`：
  - 中英文混合分词（ASCII 字母数字 + `_` 累积成 token 转小写；CJK Unified Ideographs U+4E00–U+9FFF 按字符 unigram；其他字符作为分隔符），零依赖。
  - `terms` 单 chunk 内去重，`term_freq` 累计真实出现次数；标题与正文同样参与分词。
  - `lookup` 用与 `build` 一致的 `normalize_query_term` 正则化输入；未命中、空串、纯标点都返回空 `Vec`，不报错。
  - 暴露 `pub(crate) fn tokenize` 给 `search` 复用，对外公共 API 不变。
- 实现 `search::search`：
  - 空查询、仅空白、仅标点、`top_k == 0` 全部返回 `AppError::Index`。
  - 词频累加评分；同分按 `chunk_id` 升序稳定排序；`SearchResult.snippet` 按 char 截断（不破坏 UTF-8），上限 120 字符；`matched_terms` 用 `BTreeSet` 去重并排序。
  - `chunks` 切片与 `index` 不一致时（chunk_id 缺失）安全跳过，不 panic。
- 单测覆盖 `docs/测试计划.md` §3.4 / §3.5 全部用例：
  - indexer 9 个：分词正向 / 标点剪枝 / `terms` 构建 / lookup 命中 / lookup 不命中 / 词频统计 / 标题参与分词 / 空 chunk 跳过 / serde 双向序列化。
  - search 10 个：排序 / `top_k` 截断 / 空查询报错 / 纯标点报错 / `top_k=0` 报错 / 无结果空 Vec / 多词累加 / 中文查询 / snippet 长度上限 / chunks 切片不一致。
- 本地校验：`cargo build` ✓ / `cargo test` 27 passed 0 failed（集成测试保持 `#[ignore]`）/ `cargo clippy --all-targets -- -D warnings` 0 warning ✓ / `cargo fmt --check` ✓。
- 未触碰其他人模块；未修改 `Cargo.toml` 与 `docs/接口设计.md`（`InvertedIndex` 字段早已声明 `pub`，`tokenize` 是 `pub(crate)` 不属于对外公共面）。

遇到的问题：

- `char` 在 `matches!` 宏中无法直接用十六进制范围匹配，需要 `ch as u32` 后再做 range pattern。
- `cargo fmt` 会把短的 `vec![...]` 折成单行，与人手写的多行风格不一致；以工具输出为准，已统一通过。
- search 与 indexer 的分词必须严格一致，否则 query 命中不到索引；最终把 `tokenize` 作为 `pub(crate)` 共享，避免在两处各写一份产生漂移。

下一步：

- 陈文涛：补 `parser::parse_document` 与 `chunker::chunk_document`，让端到端流水线打通；本人模块的单测已经做了"伪造 chunks"，集成层一旦有真数据，可直接喂入。
- 黄开轩：在 `cli::run` 串联 `scanner → parser → chunker → indexer::build → search`，启用 `tests/integration_test.rs` 的 `index_and_search_sample_notes`。

相关提交：

- 待 `feature/indexer` 分支提交。

## 2026-06-02：完成解析与切片模块（覆盖 Day 3 / Day 4，端到端打通）

已完成：

- 实现 `parser::parse_document`：
  - `fs::read` + `String::from_utf8` 校验 UTF-8，失败映射 `AppError::Parse` 并附带首个非法字节的偏移。
  - 按扩展名分流：`.md`（大小写不敏感）走 Markdown 清洗 + H1 标题提取，`.txt` 直读 + 首个非空白行作 title。
  - Markdown 清洗用 `regex` crate 做 inline 替换：image / link / bold（`**` `__`）/ italic（`*` `_`）/ inline code 全部去标记保留文本；HTML 注释剔除；行首前导（heading / list / blockquote）剥掉；代码围栏行替换为空行，围栏内代码内容保留以便索引。
  - **关键不变量：清洗后行数与原文一致**，让 chunker 行号对齐原始文件。
- 实现 `chunker::chunk_document`：
  - 按行累积 + `max_chars` 阈值切片；`max_chars == 0` 安全 clamp 到 1，避免死循环。
  - chunk ID 形式 `<file_path>#L<start>-L<end>`，1-based 行号。
  - 空文档至少返回 1 个空 chunk；单行超过 `max_chars` 时该行单独成片，不在行内继续切。
  - chunks 之间行号连续不重叠：`chunks[i+1].start_line == chunks[i].end_line + 1`。
- 单测覆盖 `docs/测试计划.md` §3.2 / §3.3 全部用例，外加端到端用例：
  - parser 11 个：TXT / 跳过空白行取 title / Markdown 标题 / inline 标记剥除 / list+blockquote 剥除 / 行数保持 / 围栏内代码保留 / 空文件 / 非 UTF-8 错误 / 真实样例 md / 真实样例 txt。
  - chunker 11 个：长文切多片 / 短文 1 片 / 空文 1 片 / 路径与标题传递 / ID 唯一 / ID 格式 / 行号连续不重叠 / `max_chars=0` 不死循环 / 单行超长安全处理 / 真实样例端到端 / **scanner→parser→chunker→indexer→search 完整流水线跑 sample_notes**（Rust + 所有权 都能命中）。
- 本地校验：`cargo build` ✓ / `cargo test` 49 passed 0 failed（集成测试 3 条仍 `#[ignore]`，等 cli/storage）/ `cargo clippy --all-targets -- -D warnings` 0 warning ✓ / `cargo fmt --check` ✓。

遇到的问题：

- **regex `\s` 默认包含 `\n`，会跨行吞换行符**：最初 `(?m)^\s*#{1,6}\s+` 匹配 `\n## ` 时把前一行的换行也吃掉，行数从 12 变成 10，破坏了"清洗后行数不变"的不变量。修复方法是把所有行首前导 regex 中的 `\s` 换成 `[ \t]`，限制为水平空白。
- **chunker 测试阈值算错了**：第一版 `chunk_id_format_*` 测试用 `max_chars=6` 期望 "abc\ndef" 合并到一个 chunk，但实际 7 字符（含换行）已超阈值，每行各成一片。把阈值调到 `8` 后符合预期。这也间接确认了切片边界条件正确。
- 没有引入 `tempfile` 依赖；parser 测试沿用 scanner 那边的零依赖 `TempDir` 风格，新建一份在 parser 单测内（避免跨模块共享 test util 牵动公共代码）。

下一步：

- 黄开轩：Day 7 的 `cli::run` 串联可以开做了 —— 整条流水线在 `chunker::tests::chunk_pipeline_should_feed_inverted_index` 里已经验证过，cli 只需把同样的串联搬到主流程并加渲染。完成后可启用 `tests/integration_test.rs::index_and_search_sample_notes`。
- 袁正泽：Day 8 的 `storage` 现在也可以做了 —— 索引和 chunks 都有真实数据可序列化测试。

相关提交：

- 待 `feature/parser` 分支提交（陈文涛同时负责 parser 与 chunker，合在一个分支里出 PR）。

## 2026-06-02：完成存储缓存与语义降级骨架（覆盖 Day 8 + Day 10 收尾）

已完成：

- 实现 `storage::save_index` 与 `load_index`（袁正泽 Day 8）：
  - `save_index` 写入临时文件 `<path>.tmp.<pid>.<nanos>` 后用 `fs::rename` 原子替换，避免被中断时留下半写的目标文件。父目录不存在时自动 `create_dir_all`。
  - 写入失败 / rename 失败时尽力清理临时文件，但清理错误不掩盖原始错误。
  - `load_index` 缺失文件返回 `AppError::Index`（按 `docs/接口设计.md` §9 约定，这是"语义级别索引未就绪"，不是底层 IO 错），JSON 损坏走 `#[from] serde_json::Error` 自动转 `AppError::Serde`。
  - 中间结构 `IndexSnapshot { index, chunks }` 公开声明，便于后续 `dump` 工具直接反序列化。
- 收尾 `semantic::NoopEngine`（袁正泽 Day 10 收尾铺路）：
  - 把 `rank` 与 `answer` 两个 `todo!()` 替换为 `Err(AppError::Semantic("semantic engine disabled".into()))`。
  - 抽出常量 `DISABLED_MSG` 让 cli 的 `ask` 命令可以在降级判断时复用相同信号。
- 单测覆盖 `docs/测试计划.md` §3.6 + §3.7：
  - storage 7 个：保存+加载数据保持 / 缺失文件报错 / 损坏 JSON 报错 / 父目录自动创建 / 覆盖写不残留旧数据 / 成功后无 .tmp 文件残留 / save→load→search 端到端联动。
  - semantic 2 个：NoopEngine.rank 返回 Semantic 错且 message 含 "disabled" / NoopEngine.answer 同上。
- 本地校验：`cargo build` ✓ / `cargo test` 58 passed 0 failed（集成测试 3 条仍 `#[ignore]`，等 cli 串联）/ `cargo clippy --all-targets -- -D warnings` 0 warning ✓ / `cargo fmt --check` ✓。

遇到的问题：

- `BufWriter::into_inner` 失败时返回的 `IntoInnerError` 不是 `io::Error`，需要用 `.into_error()` 拆出底层 IO 错；rustfmt 也会把那段折成多行，初版没注意触发了一次格式 diff。
- 原子写入路径选择：曾经考虑过用系统临时目录写入再 rename，但跨设备 rename 不一定原子。最终选择"目标同目录下临时文件"方案，rename 落在同一文件系统，POSIX 保证原子。

下一步：

- 黄开轩：Day 7 的 `cli::run` 串联现在条件齐了 —— `index` 子命令调用 `storage::save_index`，`search` 子命令调用 `storage::load_index`，`ask` 子命令在 `SemanticEngine.answer` 返回 `AppError::Semantic` 时降级展示候选片段。完成后启用 `tests/integration_test.rs` 的全部 3 条集成测试，达成 Day 7 MVP 里程碑。

相关提交：

- 待 `feature/storage` 分支提交（袁正泽统筹 storage + semantic 两块，合在一个分支里）。

## 2026-06-02：完成 CLI 串联，达成 Day 7 MVP 里程碑 2

已完成：

- 实现 `cli::run` + `run_index_at` / `run_search_at` / `run_ask_at`（黄开轩 Day 7）：
  - `index <path>`：scanner → parser → chunker(max=200) → InvertedIndex::build → storage::save_index，输出扫描数 / 切片数 / 关键词数 / 缓存路径。
  - `search <query> [--top N]`：storage::load_index → search → 渲染编号 / chunk_id（含路径与行号）/ 分数 / 匹配关键词 / snippet。
  - `ask <question>`：load_index → search 取 top=5 → NoopEngine.answer，捕获 `AppError::Semantic` 后降级展示候选片段并打印明确提示。
  - 缓存路径解析：`KNOWLEDGE_INDEX_PATH` env > 默认 `.knowledge_index.json`；纯函数 `cache_path_from_env` 让单测无需碰全局 env 状态。
  - 业务逻辑做成 `*_at(cache_path: &Path, ...)` 形式，集成测试可以传临时缓存路径调用，不污染当前目录。
- 启用并实现 `tests/integration_test.rs` 全部 3 条用例（之前 `#[ignore]`）：
  - `index_and_search_sample_notes`：用 `cli::run_index_at` + `cli::run_search_at` 跑完整子命令链，再用底层 API 复检结果集合（≥1 条命中、分数 > 0、中文查询命中）。
  - `search_after_loading_cached_index`：save → load 后在加载后的索引上做相同查询，断言 `chunk_id` 序列和分数完全一致。
  - `ask_falls_back_when_ai_unavailable`：通过 `cli::run_ask_at` 验证 AI 不可用时降级到候选片段不报错；附加一条无关问题（"kotlin coroutines"）验证零结果场景也走 Ok。
- 真实 smoke test（手动）：
  ```
  $ KNOWLEDGE_INDEX_PATH=/tmp/idx.json cargo run -- index ./examples/sample_notes
  扫描到 4 个文档 / 切分得到 9 个片段 / 索引共 327 个关键词 / 索引已保存
  $ KNOWLEDGE_INDEX_PATH=/tmp/idx.json cargo run -- search "Rust 所有权" --top 3
  → 3 条结果按分数降序排好
  $ KNOWLEDGE_INDEX_PATH=/tmp/idx.json cargo run -- ask "Rust 的所有权是什么?"
  → 提示 AI 引擎不可用 + 展示 5 条候选片段
  ```
- cli 单测 7 个：`cache_path_from_env` 三种分支（值 / 缺失 / 空字符串）+ `Cli::try_parse_from` 四种子命令解析（index、search 带 --top、search 默认 top、ask）。
- 本地校验：`cargo build` ✓ / `cargo test` 65 单测 + 3 集成测试全过 ✓ / `cargo clippy --all-targets -- -D warnings` 0 warning ✓ / `cargo fmt --check` ✓。

里程碑 2 达成（plan.md §8）：

- ✅ 用户可以对示例目录建立索引
- ✅ 用户可以搜索关键词并看到结果
- ✅ AI 不可用时 ask 命令可以降级
- ✅ 整套基础功能可用于中期演示

时间线对比 plan.md：今天是 6/2（Day 3），plan 把这块工作排在 Day 7（6/6），**领先 4 天**。

下一步：

- Day 9（黄开轩）：搜索结果关键词高亮、片段截断优化、无结果提示文案打磨。
- Day 10–11（袁正泽）：实现真实 `SemanticEngine` —— 接入 embedding API 或本地规则化语义匹配，让 `ask` 命令在配置 `AI_API_KEY` 时能给出真实 AI 回答。
- Day 12（全员）：边界测试补全（中英文混合、非 UTF-8 文件、目录权限不足等）。
- Day 13（陈文涛）：README、实验报告素材、演示脚本最终版。

相关提交：

- 待 `feature/cli` 分支提交。

## 2026-06-02：完成 Day 13 文档收尾

已完成（陈文涛主导，文档/演示脚本/报告素材一并交付）：

- **重写 `README.md`**：从"计划阶段"语气改为"MVP 完成"。新增章节：
  - 快速上手（编译、index、search、ask 四步，含真实输出示例）
  - 命令一览表与 `--help` 入口
  - 环境变量表格（`KNOWLEDGE_INDEX_PATH` 用法 + `AI_*` 预留说明）
  - 完整目录结构图（含每个 src 文件负责人标注）
  - 数据流图（CLI → scanner → ... → 渲染）
  - 测试规模分模块拆解（65 单测 + 3 集成测试）
  - 成员分工表
  - Rust 特性使用清单（所有权 / Result / trait / 模块系统等）
  - 创新点（中文 unigram 分词、行号对齐清洗、原子缓存、AI 可降级）
  - 课程提交内容清单
- **更新 `docs/演示脚本.md`**：与 MVP 实际行为对齐：
  - 每个时间段加"实际输出示例"区块，用 smoke test 真实输出填充，录制时可以一比一对照。
  - "如果 AI 功能未完成"章节改写为"AI 降级路径已实现"，强调这是设计特性不是 bug。
  - 新增"录制小贴士"（`--quiet` 抑制编译输出、预热建议、缓存复用对比）。
  - 新增"AI 增强后的演示扩展"，给未来真实 SemanticEngine 接入后的演示留接口。
- **新建 `docs/实验报告素材.md`**（约 350 行）：给组员填 docx 模板用的"原料库"，按章节组织：
  - 项目基本信息（成员、规模、测试数字）
  - 项目目标与意义、选题动机、创新点
  - 整体架构图与模块清单
  - 核心数据结构（贴出全部 5 个 struct + AppError）
  - Rust 核心特性使用 6 大类
  - 各模块设计要点（每个模块一段）
  - 测试策略（单测 / 集成 / 手动）
  - 工程规范与协作（Git 分支、commit 历史、依赖控制）
  - 遇到的问题与解决方案 4 例（Markdown regex 跨行 / rename 跨设备 / NoopEngine 信号 / shell 引号陷阱）
  - 成员贡献矩阵
  - 未来工作（Day 9-14 路线图）
- **同步 `CLAUDE.md` 项目状态段落**：从"planning / scaffolding phase, no Cargo.toml" 改为 "MVP completed, 65 unit + 3 integration tests passing"，列出剩余 Day 9-14 待办。让未来的 AI 协作上下文准确。
- 本地校验：`cargo test` 65+3 全过 ✓ / `cargo clippy --all-targets -- -D warnings` 0 warning ✓ / `cargo fmt --check` ✓（文档改动不触碰任何 .rs 代码）。

里程碑 4 部分达成（plan.md §8）：

- ✅ README 可以指导他人运行项目
- ✅ 报告素材覆盖需求、设计、实现、测试、分工
- ✅ 演示流程清晰、有真实输出对照
- ⏳ 演示视频本身（待录制，Day 14）

下一步候选（按必要性）：

- **Day 9**（黄开轩）：搜索结果关键词高亮 + snippet 围绕匹配位置取上下文 + ANSI 着色 → 演示视频更好看。
- **Day 10–11**（袁正泽）：接入真实 `SemanticEngine`（embedding API / 本地模型）→ 创新点加分。
- **Day 12**（全员，谭张锐可主导 scanner 边界深度）：补符号链接 / 权限不足 / BOM / 中文文件名 / 超大文件 等边界测试 → 稳定性。
- **Day 14**：录制 5 分钟演示视频 → 必交付物。

相关提交：

- 待 `feature/docs` 分支提交（陈文涛在 plan.md §5 同时负责模块和文档）。

## 后续记录模板

```text
## YYYY-MM-DD：当天主题

已完成：

- 

遇到的问题：

- 

下一步：

- 

相关提交：

- 
```
