项目简介

本示例资料用于测试“基于 Rust 的本地知识库语义搜索系统”。

系统的基础目标是扫描本地 Markdown 和 TXT 文档，解析文本内容，切分为文档片段，并构建关键词索引。用户可以通过命令行输入关键词，系统返回最相关的文档片段。

增强目标包括索引缓存、搜索结果高亮、自然语言问答和语义检索。语义检索可以通过 embedding 或 AI API 实现，但项目必须保证在没有 AI 服务时仍然可以运行。

这个项目适合体现 Rust 的模块化设计、所有权与借用、Result 错误处理、HashMap 数据结构、测试和工程协作。
